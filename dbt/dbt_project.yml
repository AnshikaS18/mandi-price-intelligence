name: "mandi_price_intelligence"
version: "1.0.0"
config-version: 2

profile: "mandi_price_intelligence"

model-paths: ["models"]
test-paths: ["tests"]
seed-paths: ["seeds"]
macro-paths: ["macros"]

target-path: "target"
clean-targets:
  - "target"
  - "dbt_packages"

models:
  mandi_price_intelligence:
    staging:
      +materialized: view
    marts:
      +materialized: table
